This is an experiment
- using go-jet for database access
- (partial) setup to be used inside project IDX
- templ package
- htmx


# Get started
- Copy the .env.dist file to .env and adjust as you wish.
- install `go install github.com/a-h/templ/cmd/templ@latest`
    - fyi: no need to put this in docker as we want the LSP anyways
    - TODO add to nix
